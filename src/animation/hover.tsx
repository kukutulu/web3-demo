import { Box } from '@mui/material';
import '../styles/hover.css';

export default function Hover() {
  return (
    <Box className="inverted-2" sx={{ width: 'fit-content' }}>
      Hover
    </Box>
  );
}
